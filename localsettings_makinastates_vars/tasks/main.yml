---
- include_jinja_vars:
    content: |-
      ---
      {% set prefix = 'corpusops_localsettings_makinastates_' %}
      {% set snamespaced, vars = vars | copsf_registry(prefix) %}
      {{ snamespaced | to_json}}
  tags: [corpusops_vars, corpusops_localsettings_makinastates_vars]
  no_log: "{{not (cops_vars_debug|default(false))}}"
