---
- include_jinja_vars:
    content: |-
      ---
      {% set prefix = 'corpusops_localsettings_hostname_' %}
      {% set snamespaced, vars = vars | copsf_registry(prefix) %}
      {{ snamespaced | to_json}}
  tags: [corpusops_vars, corpusops_localsettings_hostname_vars]
  no_log: "{{not (cops_vars_debug|default(false))}}"
