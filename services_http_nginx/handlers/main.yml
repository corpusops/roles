---
- listen: restart_corpusops_services_http_nginx
  systemd:
    daemon_reload: yes
    state: restarted
    name: "{{item}}"
  with_items: "{{corpusops_services_http_nginx_vars.services}}"
  when: "{{corpusops_services_http_nginx_vars.activated and
           corpusops_vars.has_services_manager and
           (ansible_service_mgr == 'systemd')}}"
  tags: corpusops_services_http_nginx
- listen: restart_corpusops_services_http_nginx
  service:
    state: restarted
    name: "{{item}}"
  with_items: "{{corpusops_services_http_nginx_vars.services}}"
  when: "{{corpusops_services_http_nginx_vars.activated and
           corpusops_vars.has_services_manager and
           (ansible_service_mgr != 'systemd')}}"
  tags: corpusops_services_http_nginx
