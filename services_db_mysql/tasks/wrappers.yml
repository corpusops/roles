---
- name: wrapper configs
  template:
    src: "{{item.template
            | default('../templates/'+item.name)}}"
    dest: "{{item.name}}"
    mode: "{{item.mode|default(omit)}}"
    force: "{{item.force|default(True)}}"
    owner: "{{item.owner|default(omit)}}"
    group: "{{item.group|default(omit)}}"
    trim_blocks: "{{item.trim_blocks|default(False)}}"
  with_items: "{{corpusops_services_db_mysql_vars.wrapper_configs}}"
  when: "(corpusops_services_db_mysql_vars.configs is not none and
          corpusops_services_db_mysql_vars.configs|length > 0)"
  register: corpusops_services_db_mysql_wconfigs1
- name: wrappers
  template:
    src: "../templates/usr/bin/mysqlbin-wrapper.sh"
    dest: "/usr/bin/{{item}}-{{corpusops_services_db_mysql_version}}"
    mode: "0755"
    force: "{{item.force|default(True)}}"
    owner: "{{item.owner|default(omit)}}"
    group: "{{item.group|default(omit)}}"
    trim_blocks: "{{item.trim_blocks|default(False)}}"
  with_items: "{{corpusops_services_db_mysql_vars.wrapped_binaries}}"
  when: "(corpusops_services_db_mysql_vars.configs is not none and
          corpusops_services_db_mysql_vars.configs|length > 0)"
  register: corpusops_services_db_mysql_wconfigs2
- set_fact:
    cacheable: false
    corpusops_services_db_mysql_wrappers_changed: "{{(
       corpusops_services_db_mysql_wconfigs1.changed or
       corpusops_services_db_mysql_wconfigs2.changed
    )}}"
