# corpusops.localsettings_screen ansible role
## Documentation

## Role variables
To see variables for this role, call it directly via
```bash
ansible-playbook -l LIMIT -vvv corpusops.localsettings_screen/role.yml \
   -t corpusops.localsettings_screen_vars
```
