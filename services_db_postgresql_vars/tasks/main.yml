---
- include_jinja_vars:
    content: |-
      ---
      {% set prefix = 'corpusops_services_db_postgresql_' %}
      {% set snamespaced, vars = vars | copsf_registry(prefix) %}
      {{ snamespaced | to_json}}
  tags: [corpusops_vars, corpusops_services_db_postgresql_vars]
  no_log: "{{not (cops_vars_debug|default(false))}}"
